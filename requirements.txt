# UtilityTracker — Python dependencies
#
# System packages (install via apt on Linux Mint):
#   sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-4.0
#
# Python packages (install via pip):
#   pip install -r requirements.txt

openpyxl>=3.1.0
